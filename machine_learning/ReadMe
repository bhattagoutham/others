ReadMe:

How to execute:

1) Perceptron (Voted vs Vanilla) on two datasets
>>python perceptron.py ionosphere.data
>>python perceptron.py breast-cancer-wisconsin.data

2) Fisher's LDA vs MSE
>>python lda_mse.py

3) LSA (Latent Semantic Analysis) for finding relevant documents.
>> python lsa.py train_path test_path query_doc_path

